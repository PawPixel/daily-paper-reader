---
title: "Scaling LLM Reasoning from Minimal Labels: A Semi-Supervised Framework with a Lightweight Verifier"
title_zh: 从最小标签扩展LLM推理：一种带轻量验证器的半监督框架
authors: "Keizo Kato, Chenhui Chu, Yugo Murawaki, Sado Kurohashi"
date: 2026-06-15
pdf: "https://arxiv.org/pdf/2606.16811v1"
tags: ["query:llm-papers"]
score: 9.0
evidence: 通过半监督验证从极少量标签扩展大语言模型推理学习
tldr: 当前大语言模型推理学习依赖大量正确标注答案评估推理质量，成本高。本文提出半监督框架，仅需少量标注样本训练轻量级推理正确性分类器，并利用熵过滤筛选高置信推理轨迹用于微调。在数学题和视觉编程任务上，该方法达到使用10-15倍标注数据的性能。该工作以轻量验证替代昂贵监督，为构建大规模推理资源提供实用方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.16811v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1568, \"height\": 550, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16811v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1649, \"height\": 550, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.16811v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1483, \"height\": 230, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.16811v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1461, \"height\": 228, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.16811v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1299, \"height\": 307, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.16811v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1270, \"height\": 308, \"label\": \"Table\"}]"
motivation: 现有方法需要大量正确标注答案来评估推理质量，标注成本高、可扩展性差。
method: 用少量标注训练轻量级推理正确性分类器，熵过滤筛选高置信推理轨迹微调LLM。
result: 在数学和视觉编程任务上，使用仅少量标注达到10-15倍标注数据的精度。
conclusion: 以轻量推理验证替代答案级监督，为从最小人力输入学习推理提供了可扩展范式。
---

## 摘要
对于大型语言模型（LLM）的发展，最近生成伪中间推理的方法取得了显著进展。但它们通常依赖于大量正确标注的答案来评估推理质量。本文提出了一种半监督框架，从最小监督中扩展推理学习，将推理验证本身转化为数据创建机制。我们仅使用几个标注样本训练一个轻量级的推理正确性分类器，用于判断LLM生成的中间推理轨迹是否有效。此外，基于熵的置信度阈值过滤掉不可靠样本，剩余的高置信度推理轨迹用于微调模型。在可验证数学问题（Orca-Math子集）和带视觉编程的图像场景图问答（GQA）上的实验表明，我们的方法在使用10-15倍更多标注数据时达到了可比的准确率。消融分析证实，分类器和熵过滤对于可扩展且抗噪声的伪标签都至关重要。通过用轻量级推理验证取代昂贵的答案级监督，我们的方法为构建大规模推理资源提供了一条实用途径，并为未来从最小人类输入中学习的自主推理系统铺平了道路。

## 速览
**TLDR**：现有方法生成伪推理链依赖大量正确标注。本文提出半监督框架，仅用少量标注训练轻量正确性分类器，结合熵阈值过滤低置信样本，用高置信推理迹微调LLM。在数学题和视觉问答任务中，仅需1/10-1/15标注量即达相近准确率。该方法将推理验证转化为数据生成机制，为从最小监督扩展推理能力提供实用路径。 \
**Motivation**：现有推理扩展方法需大量正确标注，成本高。本文旨在用极少量监督实现推理能力规模化学习。 \
**Method**：训练轻量推理正确性分类器，对LLM生成的推理迹打分，熵阈值过滤不可靠样本，用高置信迹微调模型。 \
**Result**：在Orca-Math和GQA上，用少量标注达到10-15倍标注数据的准确率。 \
**Conclusion**：轻量推理验证替代昂贵答案监督，实现可扩展的噪声容忍伪标注，推动少监督推理系统发展。

---

## Abstract
For the development of Large language models (LLMs), recent approaches to generating pseudo intermediate reasoning have shown remarkable progress. But they typically rely on large numbers of correctly annotated answers to assess reasoning quality. This paper presents a semi-supervised framework that scales reasoning learning from minimal supervision, turning reasoning verification itself into a data creation mechanism. We train a lightweight reasoning-correctness classifier on only a few labeled samples, which judges whether intermediate reasoning traces generated by an LLM are valid. Furthermore, an entropy-based confidence threshold filters out unreliable samples, and the remaining high-confidence reasoning traces are used to fine-tune the model. Experiments on Verifiable Math Problems (Orca-Math subset) and Question Answering on Image Scene Graphs (GQA) with Visual Programming show that our method achieves accuracy comparable to using 10-15x more labeled data. Ablation analyses confirm that both the classifier and entropy filtering are essential for scalable and noise-resistant pseudo-labeling. By replacing expensive answer-level supervision with lightweight reasoning verification, our method provides a practical path toward constructing large-scale reasoning resources and paves the way for future autonomous reasoning systems that learn from minimal human input.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：当前大语言模型（LLM）的推理能力提升依赖大量正确标注的答案来评估中间推理质量（如Chain-of-Thought）。然而，在许多实际领域（教育、法律、医学），获取大规模标注答案成本高昂，而问题文本本身却容易收集。因此，如何从极少标注样本出发，利用大量未标注问题扩展推理学习成为关键挑战。
- **整体含义**：论文提出一种半监督框架，通过训练轻量级推理正确性分类器（verifier），将推理验证本身转化为数据生成机制，从而用少量标注达到与使用10-15倍标注数据相当的性能。这为降低推理学习标注成本提供了一条实用路径。

## 2. 论文提出的方法论

### 核心思想
- **利用判别任务（验证推理正确性）比生成任务（产生正确推理）更容易**：仅用少量标注训练一个二元分类器，判断LLM生成的推理轨迹和答案是否正确；再将该分类器应用于大量未标注问题，过滤出高置信推理轨迹用于微调。
- **熵作为更可靠的置信度信号**：不直接使用分类器的原始概率（通常过于自信且校准差），而是用输出分布的信息熵度量不确定性，设定阈值筛选低熵（高置信）样本。

### 关键技术细节
1. **步骤1-3（分类器训练）**：
   - 在少量标注的问答对 `(x_i, y_i)` 上，让生成器 `M_gen` 产生推理轨迹 `r_i` 和答案 `\hat{y}_i`。
   - 用真实答案 `y_i` 标注二元正确性标签 `z_i`（true/false）。
   - 训练分类器 `M_cls`，以 `(x_i, r_i, \hat{y}_i)` 为条件，预测 `z_i`，优化交叉熵损失。

2. **步骤4-6（未标注数据过滤与微调）**：
   - 对未标注问题 `x_j`，生成器产生推理和答案。
   - 分类器预测其正确性，并计算熵 `h(p_j)`。
   - 筛选被预测为正确且熵低于阈值 `τ` 的样本作为伪标注数据 `D_pseudo`。
   - 用 `D_lab ∪ D_pseudo` 通过标准SFT微调生成器。

### 公式（文字说明）
- 训练损失：交叉熵损失，仅针对二元正确性词元（true/false）。
- 熵：`h(p) = -Σ p(v) log p(v)`，v 为词表词元。
- 伪标注选择条件：`z_j = true` 且 `h(p_j) < τ`。

### 流程图示
论文图1展示了完整的两阶段流程（分类器训练阶段与未标注推理及微调阶段）。

## 3. 实验设计

### 数据集与场景
- **任务1：可验证数学问题（Orca-Math子集）**  
  - 纯文本数学推理，需生成中间步骤和答案。
  - 数据规模：500测试、500验证；200个标注样本；10万未标注问题。
  - 评估指标：答案准确率。

- **任务2：GQA + 视觉编程**  
  - 多模态视觉问答，需生成伪代码程序（如Loc、Count、Eval）经过执行器得到答案。
  - 数据规模：1912测试、891验证；200个标注样本；约9.1万未标注问题。
  - 评估指标：最终答案准确率（由独立视觉语言模型执行）。

### 对比方法
- **零样本（Zero-shot）**：无任何微调。
- **SFT（小标注）**：仅用200个标注样本微调。
- **Khan et al. (2024) 基线**（仅用于GQA）：自训练方法，仅保留执行答案正确的伪程序。
- **SFT（大标注）**：使用3000（Orca-Math）或2000（GQA）标注样本微调，作为上界。
- **本文方法**：200标注+未标注（经分类器+熵过滤）。

### 基准（Benchmark）
- Orca-Math子集（通用数学推理）
- GQA（组合式视觉推理）+ Visual Programming 范式

## 4. 资源与算力

论文未明确说明使用的GPU型号、数量及训练时长。仅提及使用小模型（Qwen2.5-0.5B-Instruct 和 Llama3-8B-Instruct），训练细节包括批量大小、学习率等超参数，但未报告具体硬件资源和训练开销。实验规模较小，算力消耗应较低，但缺乏量化信息。

## 5. 实验数量与充分性

### 实验组数
- **Orca-Math**：零样本、SFT(200)、本文方法(200+10万未标注)、SFT(3000) 共4组；消融研究（表3）：无分类器、不同未标注量、不同过滤策略共5组。
- **GQA**：零样本、Khan基线(200)、本文方法(200+9.1万)、Khan基线(2000) 共4组；消融研究（表4）：类似5组。
- 额外分析：图2展示了熵过滤与概率过滤的精度对比（两种数据集各2条曲线）。
- 总计约15组对比实验，覆盖两个不同模态任务，且包含充分的消融和超参数探索。

### 充分性评价
- **优点**：实验设计清晰，对照严谨，验证了分类器和熵过滤两个核心组件的必要性；覆盖了文本和多模态场景，证明方法具有一定泛化性；实验结果与数据量、过滤比例的关系分析详细（图2）。
- **不足**：模型规模较小（0.5B和8B），未在更大模型（如70B）上验证；数据集仅两个，不够多样（如缺乏代码、法律、医学等）；标注数量仅测试200这一种规模，未探索不同标注量下的表现（如50、500）；未与其他半监督方法（如STaR、Meta-CoT）直接比较。因此充分性中等偏上，但仍有提升空间。

## 6. 论文的主要结论与发现

1. **极少量标注即可训练有效分类器**：200个标注样本训练的推理正确性分类器，能显著提升伪推理质量。
2. **熵过滤比原始概率过滤更可靠**：图2显示熵过滤在高置信区间（前10%）精度急剧上升，而概率过滤几乎无改善。
3. **方法达到10-15倍标注数据的效果**：Orca-Math上，200标注+未标注达到30.1%准确率，接近SFT(3000)的30.6%；GQA上，200标注+未标注达到54.1%，接近SFT(2000)的54.5%。
4. **分类器和熵过滤均不可或缺**：消融实验表明，无分类器的朴素自训练与零样本基本持平，而无熵过滤时性能提升有限或易饱和。
5. **验证比生成更易学习**：结果支持了“判断推理正确性”是比“生成正确推理”更简单的子任务这一假设。

## 7. 优点

- **方法简洁高效**：仅需少量标注训练轻量分类器，无需额外人力标注推理过程，实用性强。
- **理论支撑扎实**：结合认知心理学（识别/验证 vs 自由回忆）和校准研究（熵比概率更稳定），设计有据可依。
- **实验设计严谨**：包含两大不同任务、消融分析、过滤策略对比；图2对熵过滤有效性的可视化直观有力。
- **可扩展性好**：分类器可作为独立模块集成到现有自验证或强化学习框架中。
- **符合低资源场景**：使用最小模型（0.5B）和极低标注量，证明了方法的可行性，适合资源受限环境。

## 8. 不足与局限

- **实验规模有限**：仅在0.5B和8B模型上测试，未在更大参数规模（如70B、130B）验证；数据集仅两个，缺乏代码、法律、医学等多样化领域。
- **标注量单一**：仅测试了200标注量，未探索更小（如50）或更大（如500、1000）条件下的表现。
- **缺少与现有半监督方法的直接对比**：未与STaR、Meta-CoT、Self-Refine等baseline在相同设置下比较。
- **算力资源未报告**：无法评估方法的经济成本和可复现性。
- **阈值手动设定**：熵阈值（前10%）基于经验观察手动确定，未提出自适应或自动选择方法。
- **潜在偏差风险**：分类器仅用少量标注训练，可能存在对特定类型推理的偏好，导致过滤后数据多样性不足。
- **仅涵盖文本和结构化程序推理**：未涉及自由形式多模态推理或更具创造性的推理场景。
- **理论分析较浅**：虽然引用了一些相关研究支持，但未提供严谨的理论证明或误差界分析。

（完）
