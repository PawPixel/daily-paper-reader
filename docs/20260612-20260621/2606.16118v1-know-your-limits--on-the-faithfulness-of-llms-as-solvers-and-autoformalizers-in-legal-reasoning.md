---
title: "Know Your Limits : On the Faithfulness of LLMs as Solvers and Autoformalizers in Legal Reasoning"
title_zh: 认清局限：论LLMs作为法律推理中的求解器与自动形式化工具的忠实性
authors: "Olivia Peiyu Wang, Sanna Wong-Toropainen, Daneshvar Amrollahi, Ryan Bai, Tashvi Bansal, Arush Garg, Leilani H. Gilpin"
date: 2026-06-15
pdf: "https://arxiv.org/pdf/2606.16118v1"
tags: ["query:llm-papers"]
score: 9.0
evidence: 研究LLM在法律推理中的忠实性
tldr: 大型语言模型在推理任务上表现强劲，但未必忠实于逻辑推理。本研究通过法律蕴含任务，对比纯LLM分类、基于LLM的形式推理和基于求解器的形式推理三种范式，在重新标注的ContractNLI子集上测试了五个模型。重新标注揭示了实用法律解释与严格形式蕴涵之间的系统性差距，许多合理法律推理缺乏形式基础。LLM形式推理虽提升准确率，却存在三类不忠实失败模式：范围清洗、隐式约束忽视和程序合成错误，其中范围清洗普遍存在，表明基准准确率与逻辑忠实性之间存在根本脱节。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.16118v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1425, \"height\": 413, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16118v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1436, \"height\": 857, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.16118v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 673, \"height\": 598, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.16118v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1036, \"height\": 303, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.16118v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1457, \"height\": 787, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.16118v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1489, \"height\": 270, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.16118v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 597, \"height\": 272, \"label\": \"Table\"}]"
motivation: 研究LLM在法律推理中是否进行忠实逻辑推理，还是仅进行启发式近似。
method: 在重新标注的ContractNLI子集上，比较纯LLM分类、LLM形式推理和Z3求解器推理三种范式。
result: LLM形式推理准确率最高，但存在范围清洗等不忠实模式，且普遍发生。
conclusion: LLM形式推理的准确率提升不代表逻辑忠实性，基准性能与忠实推理之间存在本质差距。
---

## 摘要
大型语言模型（LLMs）在推理任务上表现强劲，但这是否反映了忠实的逻辑推理还是启发式近似仍不明确。我们通过比较三种范式（纯LLM分类、基于LLM的形式推理、使用Z3 SMT求解器的基于求解器的形式推理），在五个LLM上对ContractNLI的一个重新标注子集研究了法律蕴含中的这个问题。我们的重新标注揭示了语用法律解释与严格形式蕴含之间存在系统性且可测量的差距，其中相当大比例的法律上合理的推断在没有额外未明示假设的情况下并不具有形式基础。虽然引入形式结构提高了准确性（基于LLM的形式推理取得了最高的基准性能），但我们证明这种提升并不意味着忠实的推理。我们识别出三种反复出现的故障模式：范围洗白（scope laundering），即LLM在未执行底层形式推理的情况下报告与求解器不一致的分类，产生看似逻辑合理但实际上并非如此的结论；隐式约束盲点（implicit constraint blindness），即LLM忽略形式表示中存在的逻辑约束；以及程序合成失败（program synthesis failures），即LLM尽管有结构化的提示仍生成错误的Z3代码。关键的是，范围洗白在所有的模型中持续存在，这引发了对于基于LLM的形式推理作为符号执行代理的忠实性的严重担忧。这些结果揭示了基准准确性与逻辑忠实性之间的根本差距。

## 速览
**TLDR**：法律推理中，LLM的高基准性能是否反映忠实逻辑推理？通过比较纯LLM分类、LLM形式推理和Z3求解器三种范式，重新标注ContractNLI子集发现，法律解释与严格形式蕴涵存在系统差距。LLM形式推理虽精度最高，但存在范围洗白（报告与求解器不一致的结论）、隐式约束盲和程序合成失败等失败模式，且范围洗白普遍存在。这表明基准精度与逻辑忠实性之间存在根本鸿沟，质疑了LLM形式推理的可靠性。 \
**Motivation**：探究LLM在法律推理中是否执行忠实逻辑推理，而非启发式近似。 \
**Method**：在ContractNLI子集上，对比纯LLM、LLM形式推理与Z3求解器三种范式，并识别失败模式。 \
**Result**：发现法律解释与形式蕴涵存在系统差距；LLM形式推理存在三种失败模式，范围洗白普遍存在。 \
**Conclusion**：高基准精度不代表逻辑忠实性，二者存在根本差距。

---

## Abstract
Large Language Models (LLMs) achieve strong performance on reasoning tasks, but whether this reflects faithful logical inference or heuristic approximation remains unclear. We study this question in legal entailment by comparing three paradigms, including pure LLM classification, LLM-based Formal Reasoning, and solver-based Formal Reasoning using the Z3 SMT solver, on a re-annotated subset of ContractNLI across five LLMs. Our re-annotation reveals a systematic and measurable gap between pragmatic legal interpretation and strict formal entailment, where a substantial proportion of legally sound inferences are not formally grounded without additional unstated assumptions. While introducing formal structure improves accuracy, with LLM-based Formal Reasoning achieving the highest benchmark performance, we show that this gain does not imply faithful reasoning. We identify three recurring failure modes: scope laundering, where LLMs report solver-inconsistent classifications without executing the underlying formal reasoning, producing conclusions that appear logically grounded but are not; implicit constraint blindness, where LLMs overlook logical constraints present in formal representations; and program synthesis failures, where LLMs generate incorrect Z3 code despite structured prompting. Critically, scope laundering persists across all models, raising serious concerns about the faithfulness of LLM-based formal reasoning as a proxy for symbolic execution. These results reveal a fundamental gap between benchmark accuracy and logical faithfulness.

---

## 论文详细总结（自动生成）

# 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：大型语言模型（LLMs）在推理任务上表现强劲，但这种高性能是否来自于忠实的逻辑推理，还是基于启发式近似或表面模式匹配？特别是在法律等高 stakes 领域，推理的忠实性（faithfulness）至关重要。
- **研究背景**：现有 LLM 存在幻觉、虚假引用、不可验证推理等问题；链式思维（CoT）虽提升性能但未必忠实。神经符号方法通过自动形式化（autoformalization）结合符号求解器试图保证推理正确性，但面临法律文本固有的不完整性和隐含假设的挑战。
- **整体含义**：论文旨在系统评估 LLM 在法律蕴含任务中作为求解器与自动形式化工具的忠实性，揭示基准准确率与逻辑忠实性之间的根本差距，指出即使形式化结构提升性能，也可能掩盖不忠实的推理行为。

# 2. 方法论：核心思想、关键技术细节

- **核心思想**：对比三种推理范式（纯 LLM 分类、基于 LLM 的形式推理、基于 SMT 求解器的形式推理），通过设计严格的实验和错误分析，检验 LLM 是否真正依据逻辑规则（而非启发式）得出分类结论。
- **关键技术细节**：
  - **重新标注数据集**：采用严格形式蕴含定义（P ∧ ¬H 不可满足为蕴含，P ∧ H 不可满足为矛盾，其余为中性），手动重新标注 400 个 ContractNLI 样本，并构造 210 个最小对增广，最终得到 610 个样本。
  - **神经符号流水线**：
    - **自动形式化器（Autoformalizer）**：用结构化提示引导 LLM 依次执行（1）相关条款识别、（2）谓词规范化、（3）形式化翻译（将前提和假设转为 FOL，并显式编码法律合同中的假设）、（4）全局约束施加（禁止退化解、鼓励可解释谓词）。
    - **求解器（Solver）**：使用 Z3 SMT 求解器对 FOL 表示执行可满足性检查，得到逻辑意义下的蕴含/矛盾/中性分类。
    - **LLM 形式推理**：将自动形式化后的 FOL 表示输入同一 LLM，要求其输出分类（相当于让 LLM 扮演求解器，但实际未执行 Z3 代码）。
- **三种范式定义**：
  - **Simple**：纯 LLM 分类，仅用标注指南作为提示。
  - **Structured**：LLM 分类，使用与 LLM 形式推理相同的提示（包含形式化步骤），但直接输出分类（不生成代码）。
  - **LLM-Formal Reasoning**：LLM 自动生成 FOL 表示，然后对 FOL 进行推理分类（prompt 中要求其执行求解器操作，但模型实际并不调用外部求解器）。
  - **Solver-Formal Reasoning**：LLM 生成的 Z3 代码经由多轮修正后，由 Z3 求解器执行得到分类。

# 3. 实验设计

- **数据集**：基于 ContractNLI（607 份真实 NDA 合同），但重新标注为严格形式蕴含标签；最终用于评估的样本数为 610（400 个原始重新标注 + 210 个增广最小对）。
- **基准（Benchmark）**：没有外部基准，论文本身定义三种范式之间的对比。
- **对比方法**：
  - **Simple**：纯 LLM 分类。
  - **Structured**：带结构化提示的 LLM 分类（不生成 FOL）。
  - **LLM-Formal Reasoning**：LLM 在自身生成的 FOL 基础上推理分类。
  - **Solver-Formal Reasoning**：Z3 求解器基于自动生成的 FOL 进行分类。
- **被评估模型**：5 个 LLM：GPT-OSS-120B、Claude-Sonnet-4-6、Meta-Llama-3.1-8B、DeepSeek-V3.2、Qwen2.5-72B。
- **主要指标**：准确率（表3）、各类错误方向比例（表4）、SMT 代码错误率及无效代码率（表5）、注释者一致性（表2）。

# 4. 资源与算力

- **明确说明**：论文未提及训练或实验所使用的具体 GPU 型号、数量、训练时长等算力信息。仅列出了模型名称，但模型本身是预训练/API 访问的，论文更关注推理阶段的评估而非训练。因此算力细节缺失。

# 5. 实验数量与充分性

- **实验数量**：
  - 一个主数据集（ContractNLI 重新标注子集），共 610 样本。
  - 三种范式 × 五个模型 × 多次分析（准确率、错误方向、代码错误率、注释者一致性）。
  - 额外的消融分析：通过最小对增广探究隐含假设的作用；通过代码修正流水线评估程序合成质量。
  - 未进行跨数据集验证（仅一个领域和法律类型）。
- **充分性评价**：
  - **优点**：多模型、多方法对比全面；不仅仅是整体准确率，还深入分析了错误模式（方向、类型）；对自动形式化代码质量进行了系统评估。
  - **不足**：样本量小（仅数百），且全部来自 NDA 合同，领域和文本风格单一。注释者间一致性 κ=0.627（尚可但非完美），且存在主观性。未在更大规模或不同法律类型（如税法、保险）上验证。缺乏与人工法律专家推理的直接对比。实验未严格可控计算随机种子等影响（LLM 输出可能有变化性）。因此，实验结果具有启示性但不够充分，推广性受限。

# 6. 主要结论与发现

1. **形式结构提升性能但不提升忠实性**：LLM-Formal Reasoning 在准确率上普遍优于 Simple 和 Structured，但通过错误分析发现性能提升主要来自隐式假设注入，而非真正的符号执行。
2. **三大不忠实失败模式**：
   - **范围洗白（Scope Laundering）**：LLM 报告与 Z3 求解器矛盾的结果（常见于 LLM 判定蕴含/矛盾，而求解器输出中性），暗示模型直接基于整体文本做直觉推理，而非实际求解 FOL。该模式在所有模型中广泛存在（15.3%–52.5%）。
   - **隐式约束盲点（Implicit Constraint Blindness）**：LLM 未能利用 FOL 表示中编码的逻辑约束（例如存在量词下的全称量化约束），导致分类错误。
   - **程序合成失败（Program Synthesis Failures）**：LLM 生成的 Z3 代码在语法、类型、作用域等方面错误率高，即使经过多轮迭代修正（表5：Claude 25.5% 错误率、DeepSeek 54.7% 等）。
3. **假设歧义是核心挑战**：实用法律解释与严格形式蕴含之间的差距普遍存在；人类注释者和 LLM 都难以准确判断哪些隐含假设是合理的。
4. **求解器的保守性与 LLM 的过度推断**：求解器更倾向中性（因缺乏显式约束），而 LLM 更倾向蕴含（通过自动补全假设）。两者之间的分歧体现了准确率与忠实性的权衡。

# 7. 优点

- **问题聚焦独特**：直接挑战 LLM 形式推理忠实性的核心假设，而非单纯追求 benchmark 性能。
- **方法论严谨**：通过重新标注、严格错误分类、故障模式命名与分析，提供了可复现的评估框架。
- **多方面评估**：不仅比较准确率，还深入剖析错误方向、代码质量、注释者一致性，揭示隐藏的不忠实行为。
- **增量贡献**：识别并命名了“范围洗白”“隐式约束盲点”等新概念，为后续研究提供了分析工具。
- **实际意义**：对法律 AI 的高风险部署有直接警示作用，强调不能仅凭准确率判断系统可靠性。

# 8. 不足与局限

- **数据集限制**：仅 610 个 NDA 合同样本，规模小、领域单一，可能无法反映其他法律文本（如法规、判例）的复杂度。标签分布不均衡（中性占主导）。
- **注释者主观性**：即使采用严格定义，在边缘案例（蕴含 vs. 中性）上仍有 19% 的注释者差异，说明标签质量受个体解读影响。
- **自动形式化依赖手动修正**：大量 Z3 代码需要人工干预才能执行（尤其非 Claude 模型），降低了实验的完全自动化程度，且手动修正可能引入偏差。
- **LLM 输出可变性**：未报告多次运行或随机种子设置，单个样本的 LLM 输出不稳定可能影响结论稳健性。
- **缺乏纵向对比**：未与专业法律逻辑系统（如 Catala、Stipula）或人类专家推理的实际结论作对比。
- **算力与实现细节不透明**：缺少成本信息，阻碍复现与拓展。
- **外推局限性**：作者承认结果可能局限于所选模型、提示策略和 FOL 形式；更丰富的逻辑系统（如道义逻辑）可能带来不同发现。

（完）
