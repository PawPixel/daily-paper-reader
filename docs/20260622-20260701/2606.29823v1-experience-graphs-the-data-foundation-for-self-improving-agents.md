---
title: "Experience Graphs: The Data Foundation for Self-Improving Agents"
title_zh: 经验图：自我改进智能体的数据基础
authors: "Gang Liao, Yujia He, Abdullah Ozturk, Zhouyang Li, Ying Wang, Zhitong Guo, Hongsen Qin, Yaobin Qin, Tao Yang, Zewei Jiang, Dianshi Li, Jort Gemmeke, Jiangyuan Li, Liyuan Li, Nathan Yan, Masha Basmanova, Uladzimir Pashkevich, Matt Steiner, Pedro Pedreira, Rob Fergus, Anirudh Goyal, Carole-Jean Wu, Gaoxiang Liu, Andrew Witten, Daniel J. Abadi"
date: 2026-06-29
pdf: "https://arxiv.org/pdf/2606.29823v1"
tags: ["query:self-evolve"]
score: 9.0
evidence: 自我改进代理的数据基础
tldr: "现有智能体框架将探索过程中的经验视为可丢弃状态，导致崩溃后无法恢复、跨用户查询困难。本文提出Trellis，将经验图作为一等公民的数据库状态，利用数据库访问模式管理搜索过程：前沿选择是查询，跨会话复用是向量检索，训练数据提取是物化视图。在Meta的KernelEvolve中实现约10倍加速和52%更低token成本。Trellis将推理时搜索从一次性计算转变为可累积的机构资产。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.29823v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1616, \"height\": 582, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29823v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1557, \"height\": 425, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29823v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 866, \"height\": 453, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29823v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 742, \"height\": 347, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.29823v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1743, \"height\": 380, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29823v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1805, \"height\": 457, \"label\": \"Table\"}]"
motivation: 长周期智能体任务中，探索产生的经验图目前被当作临时状态，无法持久化、查询或用于训练，限制了系统的可靠性和知识复用。
method: 提出Trellis数据库系统，将经验图作为可查询、可管理的数据库状态，通过查询、向量检索、物化视图等数据库模式支持智能体搜索。
result: "在Meta的KernelEvolve上实现跨会话复用，达到约10倍加速，同时token成本降低52%。"
conclusion: 经验图数据库化使智能体变为无状态计算，自然支持崩溃恢复和水平扩展，并将推理搜索转化为可累积的持久资产。
---

## 摘要
数据库领域通过认识到新工作负载需要新系统架构，屡次推动技术前沿。我们认为，长周期智能体任务——代码生成、科学发现、硬件设计——正是这样一种工作负载。这些智能体会进行探索：它们生成工件、执行工具、观察失败、分支和修复，历经数百步。这种搜索产生了一个我们称之为经验图的结构化对象：可执行工件、工具输出、奖励、兄弟比较和因果谱系。然而，现有的智能体框架将这种经验视为一次性状态——JSON检查点和会话日志，崩溃后无法恢复，不能跨用户查询，也无法物化为训练数据。我们提出Trellis：一个将经验图视为一等公民、可管理、可查询的数据库状态的数据基础。核心洞察是：对经验图的搜索是一种数据库访问模式。前沿选择是一种查询，跨会话复用是向量种子图检索，训练数据提取是物化视图，而重建智能体在任意过去步骤所知道的内容是时间旅行查询。当数据库拥有经验图时，智能体变成无状态计算，崩溃恢复、水平扩展和闭环训练飞轮成为架构副产物。我们将设计落地于Meta的生产加速器内核优化器KernelEvolve，在那里跨会话复用实现了目标加速比约10倍，而令牌成本降低了52%。更广泛地说，Trellis将推理时搜索从一次性计算转变为持久化的机构资产：日志使数据库可靠；经验图可能使智能体可累积。

## Abstract
The database community has repeatedly advanced the state of the art by recognizing that new workloads demand new system architectures. We argue that long-horizon agentic tasks -- code generation, scientific discovery, hardware design -- are such a workload. These agents explore: they generate artifacts, execute tools, observe failures, branch, and repair over hundreds of steps. This search produces a structured object we call an experience graph: executable artifacts, tool outputs, rewards, sibling comparisons, and causal lineage. Yet existing agent frameworks treat this experience as disposable state -- JSON checkpoints and session logs that cannot be recovered after a crash, queried across users, or materialized into training data. We propose Trellis: a data foundation that treats the experience graph as first-class, governed, queryable database state. The core insight is that search over experience graphs is a database access pattern. Frontier selection is a query, cross-session reuse is vector-seeded graph retrieval, training-data extraction is a materialized view, and reconstructing what an agent knew at any past step is a time-travel query. When the database owns the experience graph, agents become stateless compute, and crash recovery, horizontal scaling, and a closed-loop training flywheel emerge as architectural byproducts. We ground the design in KernelEvolve, a production accelerator-kernel optimizer at Meta, where cross-session reuse reaches a target speedup roughly 10x faster at 52% lower token cost. More broadly, Trellis turns inference-time search from disposable computation into a durable institutional asset: logs made databases reliable; experience graphs may make agents cumulative.

---

## 论文详细总结（自动生成）

## 论文详细中文总结

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现有智能体框架（如Claude Code、Codex等）在执行长周期、探索性任务（如代码生成、科学发现、硬件设计）时，会生成大量包含可执行工件、工具输出、奖励、因果谱系的结构化数据——即“经验图”（Experience Graph）。然而，这些经验图被当作一次性状态（Python对象、JSON检查点、会话日志）存储，导致：
  - 崩溃后无法恢复；
  - 无法跨用户或跨会话查询和复用；
  - 难以系统性地提取为训练数据（需脆弱的事后抓取）；
  - 智能体状态绑定于进程，阻碍了水平扩展和无状态计算。
- **整体含义**：论文主张将经验图视为一等公民、可管理、可查询的数据库状态，而不是临时日志。通过将搜索模式映射为数据库访问模式（前沿选择=查询，复用=向量检索，训练数据提取=物化视图），可以自然获得崩溃恢复、水平扩展、闭环训练飞轮等架构收益，从而将推理时搜索从一次性计算转变为持久的机构资产。

### 2. 论文提出的方法论：核心思想、关键技术细节、算法流程

- **核心思想**：设计一个名为Trellis的数据基础，将经验图持久化为可查询的数据库状态。核心洞察是：智能体对经验图的搜索是一种数据库访问模式。
- **关键技术细节**：
  - **双循环架构**：内循环（Inner Loop）执行单个步骤：消费任务描述和上下文→生成候选工件→执行和评估→持久化结构化输出；外循环（Outer Loop）基于搜索策略决定下一步（如MCTS、演化搜索、贪心搜索等）。外循环是一个控制平面，不同策略只是对同一数据子层的不同决策策略。
  - **物理架构**：分离计算与存储。查询和执行引擎是Axiom（基于Velox），支持SQL、Cypher图查询和向量检索的融合。后端包括操作存储（<50ms响应）、向量索引、列式仓库。逻辑模式具有四级层次：tasks、sessions、nodes、prompt_history。大工件存储在对象存储中。
  - **跨会话复用**：通过向量种子图检索实现：先通过语义相似性找到先前任务，通过关系链接找到会话和最高适应度节点，再进行图遍历获取完整优化轨迹。注入率p可调，权衡收敛速度与探索多样性。
  - **训练数据物化**：SFT轨迹、DPO偏好对、GRPO组均作为物化视图提取。多版本日志支持“as-of”状态重构，确保训练数据不泄露未来信息。
  - **并发多智能体搜索**：MCTS反向传播时，多个工作线程对共享祖先的访问计数更新需原子操作，论文提出容忍最终一致性，但节点插入需要持久性——这是一个混合隔离级别。
- **算法流程**（文字说明）：
  - 智能体执行一个步骤：外循环从图数据库中读取当前前沿节点（通过查询），组装上下文，调用内循环（LLM+沙箱执行），然后将新节点插入数据库。MCTS时还要反向传播更新祖先的访问计数和累积奖励。
  - 复用步骤：新任务到来时，先对任务描述做向量检索，找到相似任务，然后通过图遍历获取其最佳子树，注入为新会话的初始上下文。
  - 训练数据提取：使用Cypher模式匹配提取父子对或兄弟对，配合SQL窗口函数计算相对优势。

### 3. 实验设计：数据集/场景、benchmark、对比方法

- **场景/数据集**：论文以Meta的生产环境内核优化系统KernelEvolve为实例。KernelEvolve使用MCTS和演化搜索生成针对AI硬件的优化内核（NVIDIA、AMD、MTIA、CPU）。数据基础积累了大量跨任务和会话的经验图。
- **Benchmark**：比较了“无记忆”（baseline，即有状态进程内存储）与Trellis数据库存储下的性能。固定模型、步骤预算（100步）、工作线程数、贪心搜索策略。注入率p=0.1和p=0.5作为对比。
- **对比方法**：主要对比无记忆（baseline）和有记忆（cross-session memory，CS）两种配置，其中有记忆配置又分为p=0.1和p=0.5两种注入率。未与其他已有数据库系统直接对比（论文声称现有系统均不满足此工作负载）。

### 4. 资源与算力

- **论文未明确说明使用的GPU型号、数量、训练时长等具体算力信息**。实验基于Meta生产环境，但只给出了步骤计数（约100步/会话）和会话数量（每个配置3个独立会话以控制方差）。对于训练数据物化的环节，也未提及训练所需算力。因此，资源与算力细节缺失。

### 5. 实验数量与充分性

- **实验数量**：
  - 报告了三项主要测量：恢复与无状态性（定性）、跨会话复用效果（定量）、训练数据提取（定性）。
  - 跨会话复用实验中，每个配置（baseline、CS p=0.1、CS p=0.5）进行了3个独立会话，总计9个会话。结果取平均，并报告了趋势一致性。
  - 实验维度包括：最优适应度随步骤变化、有效节点vs.有缺陷节点比例、令牌成本节省、探索多样性（策略组合数量）。
- **充分性评价**：
  - **优点**：实验抓住了关键指标（加速比、缺陷率、令牌成本、多样性），并显示了注入率的影响，揭示了探索-锚定权衡。控制方差的做法合理。
  - **不足**：实验规模较小（仅9个会话，每个约100节点），未进行大规模统计显著性检验。未与其他系统基线对比（如使用传统文件检查点或向量数据库的代理）。未验证训练数据物化部分的实际训练效果（论文只说明了可行性，未给出训练后模型性能）。此外，KernelEvolve是唯一实例，其他领域（硅验证、药物发现）仅提及其可迁移性而无实验数据。因此，实验在覆盖范围和统计强度上不够充分，但作为早期测量可接受。

### 6. 论文的主要结论与发现

- **恢复与无状态性**：数据库原生状态使崩溃恢复自然实现，无需检查点反序列化，任何工作线程可从最后提交节点恢复。
- **跨会话复用**：
  - 相比无记忆基线，有记忆配置下缺陷节点率从55%降至34%（p=0.1）和21%（p=0.5）。
  - 有效节点中达到基线加速比的比例从79.5%升至90.8%和100%。
  - 达到1.2×加速比所需的步骤数从基线51步降至约5步——10倍加速。
  - 令牌成本每有效节点降低52%（主要来自避免缺陷节点导致的调试重试循环）。
  - 但高注入率（p=0.5）导致搜索锚定：策略组合数从20降至8，最佳解不如无记忆（1.36× vs 1.49×）。p=0.1在收敛速度和解质量之间取得较好平衡。
- **训练数据物化**：无需重新rollout，每个生产会话同时是服务操作和训练数据生成器，具备完整谱系。
- **领域通用性**：Trellis通过更换适应度函数和技能即可从内核优化迁移到硅验证，证明其领域无关性。

### 7. 优点：方法或实验设计上的亮点

- **思想创新**：首次系统性地将智能体探索产生的经验图定义为数据库工作负载，并设计了完整的数据库架构（逻辑模型、物理架构、查询层、物化视图、版本控制），具有前瞻性。
- **形式化映射**：将智能体操作精确映射为数据库查询模式（表2），为后续系统设计提供了清晰蓝图。
- **跨会话重用实验**：定量展示了记忆注入率对加速、缺陷率、多样性的影响，并揭示了关键的探索-锚定权衡，为检索策略设计提供了实证基础。
- **训练数据物化的设计**：利用数据库的物化视图和版本日志，保证训练数据的谱系和准确性（AS-OF状态），避免了重rollout成本。
- **实际部署验证**：基于Meta生产系统KernelEvolve，表明方案在真实大规模环境中可行。

### 8. 不足与局限

- **实验规模有限**：仅有9个会话的实验数据，缺乏统计显著性检验和更多场景验证（如其他领域、其他搜索策略、大并发）。
- **缺乏与现有系统的直接对比**：未比较Trellis与基于文件检查点、传统数据库或向量数据库的性能差异。
- **训练数据环节未训练模型**：论文仅描述了物化过程，未展示实际训练后的模型性能提升。
- **一致性模型未完全解决**：对并发树搜索的混合隔离级别（读未提交vs持久性）缺乏正式分析和性能影响评估。
- **局限性**：
  - 主要针对长周期探索任务，不适用于简单的一次性查询任务。
  - 对技能和声明性记忆的管理仍依赖文件系统（FUSE），统一性有待加强。
  - 治理需求未深入讨论（如源节点撤回如何影响物化视图）。
  - 未评估数据库本身在高吞吐（大量并发智能体）下的性能瓶颈。

（完）
