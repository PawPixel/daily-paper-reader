---
title: "Rethinking Reward Supervision: Rubric-Conditioned Self-Distillation"
title_zh: 重新思考奖励监督：基于评分准则的条件化自蒸馏
authors: "Siyi Gu, Jialin Chen, Sophia Zhou, Arman Cohan, Rex Ying"
date: 2026-06-17
pdf: "https://arxiv.org/pdf/2606.19327v1"
tags: ["query:on-policy"]
score: 10.0
evidence: 基于评分标准的有条件自蒸馏用于在策略蒸馏
tldr: 针对推理语言模型的后训练中，常用方法存在蒸馏依赖昂贵且可能噪声的思维链标注、强化学习将反馈压缩为标量信号而缺乏细粒度指导的问题。本文提出Rubric-Conditioned Self-Distillation，利用准则作为结构化细粒度反馈，在在线自蒸馏中提供token级指导。在科学推理基准上，该方法平均超过GRPO 1.0分、OPSD 0.9分。贡献在于将准则级标准有效转化为推理过程的token级信用分配。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.19327v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 696, \"height\": 514}, {\"url\": \"assets/figures/arxiv/2606.19327v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1440, \"height\": 539}, {\"url\": \"assets/figures/arxiv/2606.19327v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1293, \"height\": 440}, {\"url\": \"assets/figures/arxiv/2606.19327v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1432, \"height\": 337}, {\"url\": \"assets/figures/arxiv/2606.19327v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1291, \"height\": 240}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.19327v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1325, \"height\": 313}, {\"url\": \"assets/tables/arxiv/2606.19327v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1261, \"height\": 204}, {\"url\": \"assets/tables/arxiv/2606.19327v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 704, \"height\": 279}, {\"url\": \"assets/tables/arxiv/2606.19327v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1230, \"height\": 225}, {\"url\": \"assets/tables/arxiv/2606.19327v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1306, \"height\": 240}, {\"url\": \"assets/tables/arxiv/2606.19327v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1242, \"height\": 314}, {\"url\": \"assets/tables/arxiv/2606.19327v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1218, \"height\": 355}]"
motivation: 现有后训练方法依赖昂贵噪声的思维链或标量奖励，缺乏细粒度反馈，难以精准改进推理过程。
method: 提出Rubric-Conditioned Self-Distillation，先学习任务准则，再训练准则引导的推理器进行在线自蒸馏。
result: 在科学推理基准上平均超越GRPO 1.0分和OPSD 0.9分。
conclusion: 准则条件自蒸馏将准则级标准转化为token级指导，实现更精细的推理过程优化。
---

## 摘要
推理语言模型的后训练通常由监督蒸馏和基于可验证奖励的强化学习驱动。蒸馏往往依赖于思维链标注，这些标注获取成本高昂且可能本身存在噪声、不完整或部分错误；即使最终答案正确，不完美的推理过程仍可能干扰学习。另一方面，基于已验证奖励的强化学习通常将评估反馈压缩为标量信号，掩盖了响应中哪些方面需要改进。我们提出**基于评分准则的条件化自蒸馏**框架，该框架将评分准则作为结构化的细粒度反馈融入在线策略自蒸馏中。我们的方法使教师模型基于准则级别的评分标准，并为学生模型自身采样的轨迹提供 token 级别的指导。这一设计避免了将单一参考推理过程作为唯一监督目标。相反，评分准则明确了强有力的响应应满足的条件，从而在推理过程中实现比标量奖励优化更细粒度的信用分配。我们通过两阶段流程实例化该框架：首先学习生成任务特定的评分准则，然后训练一个由评分准则引导的推理模型。我们在多样化的科学推理基准上评估，结果表明，基于评分准则的条件化自蒸馏有效地将准则级别的标准转化为推理过程中的 token 级指导，平均超过 GRPO 1.0 分，超过 OPSD 0.9 分。

## Abstract
Post-training of reasoning language models is commonly driven by supervised distillation and reinforcement learning with verifiable rewards. Distillation often relies on chain-of-thought annotations that are expensive to obtain and may themselves be noisy, incomplete, or partially incorrect; even when the final solution is correct, an imperfect rationale can interfere with learning. Reinforcement learning with verified rewards, on the other hand, typically compresses evaluative feedback into a scalar signal, obscuring which aspects of a response should be improved. We propose \textbf{Rubric-Conditioned Self-Distillation}, a framework that incorporates rubrics as structured, fine-grained feedback for on-policy self-distillation. Our method conditions the teacher model on criterion-level rubrics and uses it to provide token-level guidance on the student's own sampled trajectories. This design avoids treating a single reference rationale as the sole supervision target. Instead, rubrics specify what a strong response should satisfy, enabling more fine-grained credit assignment over the reasoning process than scalar reward optimization. We instantiate this framework with a two-stage pipeline that first learns to generate task-specific rubrics and then trains a rubric-guided reasoner. We evaluate on a diverse suite of science reasoning benchmarks and results show that rubric-conditioned self-distillation effectively converts rubric-level criteria into token-level guidance over the reasoning process, surpassing GRPO by 1.0 points and OPSD by 0.9 points on average.

---

## 论文详细总结（自动生成）

# 论文总结：Rethinking Reward Supervision: Rubric-Conditioned Self-Distillation

## 1. 核心问题与整体含义（研究动机和背景）

- **问题**：当前推理语言模型的后训练主要依赖两类方法：
  - **蒸馏（Distillation）**：需要昂贵的链式思维（CoT）标注，且标注本身可能噪声、不完整或部分错误，即使答案正确，不完美的推理过程也会干扰学习。
  - **强化学习（RL）**：将评估反馈压缩为标量奖励（如最终答案正确/错误），丢失了细粒度信息，无法告知模型哪些步骤需要改进。
- **动机**：需要一种既能提供密集 token 级指导、又能保留多维度质量评估信息的监督方式。现有工作（如 OPSD）虽然提供 token 级指导，但受限于单一参考轨迹；基于评分标准（rubric）的 RL 虽引入维度分解，但仍将反馈聚合为标量。
- **整体含义**：本文提出 RCSD（Rubric-Conditioned Self-Distillation），将评分标准作为结构化、细粒度的教师端监督，用于在线自蒸馏，从而将准则级标准转化为推理过程中的 token 级信用分配。

## 2. 方法论

### 核心思想
- 利用评分标准（rubric）作为教师的特权信息（priviledged information），在在线自蒸馏中提供稠密的、多维度感知的 token 级指导，而非压缩为标量奖励或参考轨迹。

### 关键技术细节
- **两阶段流程**：
  - **Stage I：学习评分标准生成器（Rubric Generator）**  
    - 教师条件：`pRT(r | x, y*)`（拥有问题 x 和参考答案 y*）  
    - 学生条件：`pRS(r | x)`（仅有问题）  
    - 训练目标：在线蒸馏，最小化学生采样的 rubric 前缀上的教师-学生 KL 散度。  
    - 结果：产生一个可用的评分标准生成器，无需推理时特权信息。
  - **Stage II：训练评分标准引导的推理器（Reasoner）**  
    - 学生策略：`pYS(y | x)`（仅问题）  
    - 教师策略：`pYT(y | x, r)`（条件于问题 x 和 Stage I 生成的评分标准 r）  
    - 训练目标：学生在自己的推理轨迹上采样，教师提供 token 级指导，最小化前向 KL 散度。  
    - 关键：使用前向 KL（Forward KL）而非反向 KL，鼓励学生覆盖教师分布，保持多样性的有效推理路径。
- **公式表示**（文字说明）：
  - Stage I 损失：`L_rubric = E_{ˆr~pRS} [ Σ_t D_KL( pRT(·|x,y*, ˆr<t) ∥ pRS(·|x, ˆr<t ) ) ]`
  - Stage II 损失：`L_reason = E_{ˆy~pYS} [ Σ_t D_KL( pYT(·|x,r, ˆy<t) ∥ pYS(·|x, ˆy<t ) ) ]`

### 算法流程（文字说明）
1. 准备训练集 D = {(x, r*, y*)}。
2. Stage I：迭代，对每个样本，学生采样 rubric 轨迹；计算教师和学生 token 分布；更新 rubric 生成器。
3. Stage II：迭代，对每个样本，用 Stage I 的生成器产生评分标准 r；学生采样推理轨迹；计算教师和学生 token 分布；更新推理器。

## 3. 实验设计

### 数据集 / 场景
- **训练数据**：
  - 评分标准生成器：基于 RaR-Science 和 RubricHub，约 10k 样本。
  - 推理器：额外使用 NaturalReasoning（过滤非空参考答案），共约 30k 样本。
- **评估基准**（六项）：
  - **可验证**：GPQA-Diamond、SciBench、PIQA、ResearchQA
  - **非可验证**：RaR-Science、RubricHub
  - **域外泛化**：MedMCQA、PubMedQA（医学问答）
- **评价指标**：各基准准确率，非可验证任务使用 GPT-4.1-mini 作为 LLM 裁判，遵循原始设置。

### 对比方法
1. **Base**：Qwen3-8B（骨干模型）
2. **+SFT**：监督微调（在蒸馏 CoT 上）
3. **+GRPO**：标准 GRPO，奖励由 LLM 裁判生成标量分数
4. **+GRPO-Rubrics**：GRPO 变体，奖励基于预生成的评分标准
5. **+OPSD**：在线自蒸馏，教师条件于参考答案
6. **+RCSD（Ours）**：本文方法

### 消融实验
- 损失类型（Forward KL vs Reverse KL vs JSD）
- 模型规模（1.7B、4B、8B）
- 评分标准来源（GT 参考 vs 生成 vs 退化变体）
- Stage I 必要性（直接使用更大模型生成评分标准 vs 学习生成器）
- 评分标准质量退化（通用、噪声、随机、减半）
- 训练步数影响

## 4. 资源与算力

- **文中未明确说明 GPU 型号和数量**，但提供了训练配置细节：
  - LoRA（r=64, α=128），优化器 AdamW，学习率 5e-6，batch size 32，使用 FlashAttention 3。
  - 评分标准生成器：训练 1000 步，最大序列长度 2048。
  - 推理器：训练 100 步（RCSD 和 OPSD），GRPO 训练 500 步，最大完成长度 4096。
  - 评估：温度 1.0，top-p=0.95，平均 4 次独立生成。
- 算力细节（如具体 GPU 型号、数量、训练时长）未提供，因此无法准确评估资源消耗。

## 5. 实验数量与充分性

- 实验较为充分，涵盖以下方面：
  - 主表：6 个基准 × 6 种方法（+ 基线），共 36 项性能数据。
  - 域外泛化：2 个医学基准。
  - 消融实验：损失类型（3 种）、模型规模（3 种）、评分标准来源（5 种退化 + 1 学习）、Stage I 必要性（3 种比较）、训练步数分析（图 5）。
  - 定性分析：案例研究（推理长度与稳定性）、失败分析（评分标准质量）。
- **公平性**：采用相同骨干模型（Qwen3-8B），训练步数、超参数尽量对齐基线（OPSD 同 100 步，GRPO 500 步因收敛更慢）。评估使用相同的温度、top-p，并多次采样平均。
- **不足**：未在多种骨干模型上验证（仅 Qwen3 系列），虽然覆盖 1.7B/4B/8B 但架构相似；未与更大模型（如 70B）对比；部分消融结果波动较小，统计显著性未严格测试。

## 6. 主要结论与发现

1. **RCSD 在 6 个基准上平均表现最佳（70.6）**，显著优于 GRPO（69.2）和 OPSD（69.7），也超越了 GRPO-Rubrics（69.6）。
2. **在非可验证任务上增益更大**：ResearchQA 和 RubricHub 提升最明显（+8.2 和 +4.9 vs 基础模型），说明结构化评分标准在开放域推理中有效。
3. **跨模型规模有效**：1.7B、4B、8B 均观察到一致提升，且较大模型受益更多。
4. **前向 KL 是最优蒸馏损失**，优于反向 KL 和 JSD，能更好保持多样性和覆盖教师分布。
5. **学习的评分标准生成器可达参考标准近似的效果**，且对评分标准质量有一定的鲁棒性（退化变体仍提升，但学习版最佳）。
6. **RCSD 不需要额外奖励模型或裁判**，属于无验证器（verifier-free）的后训练框架，适合非可验证场景。

## 7. 优点

- **创新性**：将评分标准作为特权信息融入在线自蒸馏的 token 级指导，而非像先前工作仅用于标量化奖励。
- **两阶段自动化**：Stage I 学习评分标准生成器，消除了对手动标注评分标准的依赖，且生成器可跨任务泛化。
- **理论支撑**：提供了命题 1（鲁棒性界），说明生成评分标准足够诱导相似教师分布即可。
- **实验全面**：覆盖多种任务类型（可验证/非可验证）、多种模型大小、多种损失函数、多种评分标准退化情形，并包括域外泛化和定性分析。
- **实用性强**：训练步数少（100 步），无需独立奖励模型，便于扩展到开放领域。

## 8. 不足与局限

- **算力资源不透明**：未报告 GPU 型号、数量、训练总时长，难以复制资源需求。
- **骨干模型单一**：仅使用 Qwen3 系列（1.7B、4B、8B），未在其他架构（如 LLaMA、Mistral）上验证泛化性。
- **域外泛化测试有限**：仅测试了医学领域（MedMCQA、PubMedQA），缺乏更多非科学领域的评估（如法律、商业）。
- **评分标准生成器质量局限**：定性分析显示生成器有时冗长、重叠，与参考评分标准相比不够精确，可能影响传播上限。
- **复杂推理任务未探索**：未在数学证明、代码生成等严格验证任务上评估，RCSD 在这些场景的优势可能不显著。
- **统计显著性未报告**：多次随机采样平均，但未提供置信区间或方差分析，部分增益可能不显著。
- **仅与有限基线对比**：未与 Process Reward Model（PRM）或批判性学习等方法比较，后者也提供细粒度反馈。
- **理论部分较弱**：鲁棒性界仅假设 bounded loss，未给出实际紧界或更深入的收敛保证。

（完）
