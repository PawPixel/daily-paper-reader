---
title: "RolloutPipe: Overlapping Pipelined Rollout and Training in Disaggregated On-Policy LLM Reinforcement Learning"
title_zh: "RolloutPipe: 解耦在线策略LLM强化学习中的流水线式重叠生成与训练"
authors: "Rongjian Chen, Jianmin Hu, Kejiang Ye, Minxian Xu"
date: 2026-06-25
pdf: "https://arxiv.org/pdf/2606.26997v1"
tags: ["query:on-policy"]
score: 8.0
evidence: 通过流水线重叠提升在策略LLM强化学习的效率
tldr: "面向分离式在线策略LLM强化学习，现有同步GRPO系统需等待整个rollout完成才开始训练，导致GPU空闲，而异步方法存在陈旧数据问题。提出RolloutPipe框架，通过完整组流水线（CGP）和前沿组调度（FGD）实现训练与rollout重叠，且不牺牲策略正确性。在Qwen3-1.7B四个推理基准上，端到端时间缩短30.7%-42.3%，训练等待率降低37%-76%。该工作为分离式RLVR系统提供了一种高效异步流水线方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.26997v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1194, \"height\": 360}, {\"url\": \"assets/figures/arxiv/2606.26997v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1200, \"height\": 560}, {\"url\": \"assets/figures/arxiv/2606.26997v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1097, \"height\": 763}, {\"url\": \"assets/figures/arxiv/2606.26997v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 895, \"height\": 791}, {\"url\": \"assets/figures/arxiv/2606.26997v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1242, \"height\": 547}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.26997v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1252, \"height\": 1019}, {\"url\": \"assets/tables/arxiv/2606.26997v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1271, \"height\": 451}]"
motivation: 现有同步GRPO系统等整个rollout完成才训练，GPU空闲；异步方法则存在陈旧数据问题。
method: 提出RolloutPipe，包含完整组流水线（CGP）即刻分发可训练组，以及前沿组调度（FGD）优先生成训练批次所需组。
result: "在Qwen3-1.7B上，端到端时间缩短30.7%-42.3%，训练等待率降低37%-76%。"
conclusion: RolloutPipe在保持在线策略正确性的同时实现异步重叠，显著提升了分离式RLVR系统的资源利用效率。
---

## 摘要
大语言模型（LLM）在推理方面的后训练越来越依赖于基于可验证奖励的强化学习（RLVR），其中模型从数学、逻辑和科学任务中的真实反馈中学习。为了实现灵活的资源分配并支持异构训练设置，现代RLVR系统采用解耦架构，将生成和策略训练分离到独立的GPU池中。然而，现有的同步在线策略GRPO（组相对策略优化）RLVR系统在开始训练之前完成整个生成过程，导致在生成过程中训练GPU池空闲。异步RL流水线重叠这两个阶段，但代价是在陈旧数据上进行训练。为了解决这些挑战，我们提出了RolloutPipe，一种用于解耦RLVR系统的后训练框架，它将固定权重的生成转变为一个完整组流水线，其中可训练组在后续组仍在生成时移动到训练器。RolloutPipe通过两种技术实现这一点：完整组流水线（CGP）和前沿组调度（FGD）。CGP在每个可训练完整组完成构建后立即将其发送到训练器的先进先出队列，FGD是Rollout节点上的准入策略，首先准入形成下一个训练批次所需的前沿组请求，从而使训练器就绪组更早、更稳定地到达。该设计在保持在线策略正确性的同时，在生成完成之前开始训练。在Qwen3-1.7B上，跨四个推理和科学基准以及十二种生成设置进行评估，与最先进的生成和训练系统Slime相比，RolloutPipe将生成到训练结束的时间缩短了30.7%-42.3%，并将训练器等待比例降低了37%-76%。

## Abstract
Large language model (LLM) post-training for reasoning increasingly relies on reinforcement learning with verifiable rewards (RLVR), where models learn from ground-truth feedback on mathematical, logical, and scientific tasks. To enable flexible resource allocation and support heterogeneous training setups, modern RLVR systems adopt disaggregated architectures that decouple rollout generation and policy training across independent GPU pools. However, existing synchronous on-policy GRPO (Group Relative Policy Optimization) RLVR systems finish an entire rollout before starting training, leaving the trainer GPU pool idle while rollout is still ongoing. Asynchronous RL pipelines overlap the two stages, but at the cost of training on stale data. To address these challenges, we propose RolloutPipe, a post-training framework for disaggregated RLVR systems, which turns the fixed-weight rollout into a complete-group pipeline where trainable groups move to the trainer while later groups are still being generated. RolloutPipe achieves this through two techniques including complete-group pipelining (CGP) and frontier-group dispatch (FGD). CGP dispatches each trainable complete group to the trainer FIFO as soon as group materialization finishes, and FGD is an admission policy on the Rollout node that first admits requests for the frontier groups needed to form the next training batch, so that trainer-ready groups arrive earlier and more steadily. The design starts training before the rollout completes while maintaining on-policy correctness. Evaluated on Qwen3-1.7B across four reasoning and science benchmarks and twelve rollout settings, RolloutPipe shortens the rollout-to-train-end time by 30.7%-42.3%, and lowers the trainer waiting ratio by 37%-76% compared to Slime, a state-of-the-art rollout and training system.

---

## 论文详细总结（自动生成）

# RolloutPipe：解耦在线策略LLM强化学习中的流水线式重叠生成与训练——论文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：大语言模型（LLM）后训练中，基于可验证奖励的强化学习（RLVR）日益重要。现代RLVR系统采用**解耦架构**，将生成（rollout）和策略训练分离到独立GPU池，以支持灵活资源分配。然而，现有同步在线策略GRPO（如Slime）采用“先完成生成、再开始训练”的串行模式，导致训练GPU在生成阶段长时间空闲。异步流水线虽能重叠阶段，但会使用陈旧数据（旧权重生成的数据），破坏在线策略的正确性。
- **核心问题**：如何在保持在线策略正确性的前提下，让训练在生成完成前就开始，从而消除训练GPU空闲，同时不引入数据过时问题。
- **整体含义**：RolloutPipe通过将固定权重的生成过程转变为**完整组级流水线**，使得可训练组在后续组仍在生成时即可进入训练，实现训练和生成的并行重叠，显著提升资源利用率，缩短端到端时间。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：将同步GRPO中round级别的串行执行粒度细化为**完整组（complete-group）级别流水线**。每个组（group）由同一prompt的K个响应构成，GRPO需要组内所有响应完成才能归一化计算优势，因此完整组是可训练的最小单元。RolloutPipe在生成侧和训练侧分别引入两种机制：
  - **完整组流水线（CGP）**：生成侧一旦某个组完成材料化（包括奖励计算、组统计归一化），立即将该组送入训练器FIFO，当FIFO中累积U个组（U = 全局批次大小/K）时开始一次训练更新。训练早于整个rollout完成开始，且保持权重固定，保证在线策略正确性。
  - **前沿组调度（FGD）**：在生成节点的请求准入阶段维护一个大小为Fw的前沿集合F，只准入属于F的组请求，其余组请求暂存。当F中某组完成所有K个请求后，从暂存组中选取最小序号组加入F。这样优先完成前沿组（构成下一训练批次所需的组），使训练器获得更早、更稳定的组供应。
- **技术细节**：
  - 训练侧：Megatron执行前向、反向和梯度累积；每消耗U个组触发一次优化器步骤，与串行基线相同的语义；权重刷新在所有R个组消耗完后进行。
  - 生成侧：SGLang引擎配合FGD准入控制，每组最多Fw个并发，减少组间完成时间差异。
  - 算法流程：四个函数（FGD_Admit, Rollout_Worker, CGP_Handoff, Train）分别位于三个节点，事件驱动，实现流水线重叠。
- **关键公式**：
  - 组优势计算：\( A_i = (r_i - \mu_{\text{group}}) / (\sigma_{\text{group}} + \epsilon_{\text{std}}) \)
  - 训练器等待比例：\( w_{\text{train}} = (t_{\text{start}} - t_{\text{rollout\_start}}) / (t_{\text{train\_end}} - t_{\text{rollout\_start}}) \)
  - CGP恢复的时间：\( \Delta t_{\text{CGP}} = t_{\text{complete}} - t_{\text{first}}^{(U)} \)

## 3. 实验设计：使用的数据集/场景、基准、对比方法

- **数据集/场景**：四个推理和科学工作量，来自标准基准：
  - LSAT-AR（AGIEval）：逻辑推理问题
  - Sci-XW（SciBench）：大学级数学、化学、物理
  - Sci-JL（SciBench子集）：不同问题分布
  - OlyPhys（OlympiadBench）：国际和国内数学物理奥赛题
- **基准**：以最新同步GRPO系统**Slime**作为基线。
- **对比方法**：
  - Slime（原生串行）
  - RolloutPipe (CGP) —— 仅启用CGP
  - RolloutPipe (CGP+FGD) —— 同时启用CGP和FGD
- **实验设置**：每个工作在三个组数量R（32、64、96）下运行，每个配置重复4轮，取均值与标准差。GRPO参数固定：K=8，B=16（即U=2）。FGD的Fw=U=2。共12个结果点（4个workload × 3个R）。

## 4. 资源与算力

论文明确说明：
- **策略模型**：Qwen3-1.7B（Qwen3家族最小具有推理能力的模型）。
- **训练侧**：8× RTX 4090 24GB GPU，TP=4, DP=2。
- **生成侧（Rollout节点）**：2× A100 40GB PCIe GPU，TP=2。
- **训练时长**：未给出绝对时间，但给出了各配置下的端到端时间（例如R=96时约500-1200秒）。通过重复4轮报告均值。

## 5. 实验数量与充分性

- **数量**：共4个工作量 × 3个R × 3种配置（Slime/CGP/CGP+FGD） = 36条实验运行（每个配置4轮，但结果图中每个柱为4轮均值）。此外还报告了响应长度、训练器计算时间、等待比例等附加指标。
- **充分性评价**：
  - **充分**：覆盖不同推理领域和难度，组数量从小到大，对比了CGP和CGP+FGD消融，分析了训练计算量是否改变、等待比例来源。
  - **客观公平**：所有配置共享相同超参数和基础检查点，训练计算时间几乎一致，说明加速来自重叠而非计算量减少。实验设置合理，错误线显示标准差较小，结果稳定。
  - 但模型仅测试了1.7B规模，未在更大模型上验证，作者承认这一点并表示方法设计独立于模型内部参数，预期泛化。

## 6. 论文的主要结论与发现

- RolloutPipe（CGP+FGD）相比Slime，端到端时间（rollout-to-train-end）缩短 **30.7%–42.3%**。
- 训练器等待比例从Slime的47%–52%降至14%–33%（降幅37%–76%）。
- CGP贡献了总减少量的71%–96%，且随R增大而增大（因为更多组被流水线化）。
- FGD在CGP基础上进一步缩短时间2.5%–11.4%，通过稳定前沿组供应。
- 所有加速均未改变训练计算量或响应长度，保持在线策略正确性（固定权重、同步优化步）。
- 方法对于分离式GRPO系统具有普适性。

## 7. 优点：方法或实验设计上的亮点

- **方法创新点**：首次在同步GRPO中实现组级流水线，既不引入陈旧数据，又解决了GPU空闲问题。CGP和FGD两个技术设计轻量，仅改变调度时机和顺序，不修改模型或优化器。
- **公平验证**：详细对比了CGP和FGD各自的贡献，通过响应长度和训练计算时间的一致性证明加速并非来自计算量减少。
- **分析深入**：量化了最早批次分发时刻、等待比例、组完成时间分布等，清晰解释加速来源。
- **工程实现**：基于Slime、Megatron-LM、SGLang和Ray搭建，算法复杂度分析显示开销线性，通信可重叠。

## 8. 不足与局限

- **模型规模局限**：实验仅使用Qwen3-1.7B，未验证在更大模型（如7B、13B或更大）上是否同样有效。虽然作者认为机制与模型内部参数无关，但实际中更大模型可能带来更长的生成时间和不同的调度行为，需要验证。
- **资源环境单一**：GPU集群配置固定（8×RTX 4090 + 2×A100），未在更大规模（如多节点、不同互联）下测试，也未考虑通信带宽瓶颈对流水线效能的影响。
- **未对比异步方法**：论文仅与同步基线Slime对比，未与异步RL系统（如AReaL、AsyncFlow）进行直接比较（虽然指出异步方法存在陈旧数据问题，但未提供实验数据）。
- **收敛性分析缺失**：虽然声称保持在线策略正确性且计算量相同，但未报告训练过程中的奖励曲线或收敛速度是否完全一致，可能存在隐式影响（如梯度累积顺序差异）。
- **FGD的适用性**：Fw固定为U=2，未探索不同Fw值的影响，也未讨论在U较大或请求长度差异极大时的效果。
- **应用限制**：仅针对GRPO这种基于组的强化学习方法，不适用于PPO等样本级训练方法。

（完）
