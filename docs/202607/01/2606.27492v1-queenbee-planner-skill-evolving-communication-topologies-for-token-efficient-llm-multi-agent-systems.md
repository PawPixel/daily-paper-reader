---
title: "QueenBee Planner: Skill-Evolving Communication Topologies for Token-Efficient LLM Multi-Agent Systems"
title_zh: QueenBee规划器：面向Token高效LLM多智能体系统的技能演化通信拓扑
authors: "Congjia Tian, Yuhang Yao, Jiaming Cui"
date: 2026-06-25
pdf: "https://arxiv.org/pdf/2606.27492v1"
tags: ["query:self-evolve"]
score: 9.0
evidence: 通过技能演化实现LLM多智能体系统的通信拓扑自我改进
tldr: LLM多智能体系统的性能不仅依赖个体推理，也依赖智能体间的通信拓扑。QueenBee Planner将拓扑视为可自改进的设计技能，由外LLM规划器学习生成时序通信DAG，并通过执行轨迹提炼设计规则。在聚合和分布式协调任务上，自演进生成的拓扑将RMSE从12.53降至7.87，同时减少消息和计算成本。这表明多智能体系统可学习可重用的架构设计知识。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.27492v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1379, \"height\": 741}, {\"url\": \"assets/figures/arxiv/2606.27492v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1353, \"height\": 1197}, {\"url\": \"assets/figures/arxiv/2606.27492v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1423, \"height\": 564}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.27492v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1454, \"height\": 290}, {\"url\": \"assets/tables/arxiv/2606.27492v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1457, \"height\": 405}, {\"url\": \"assets/tables/arxiv/2606.27492v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1339, \"height\": 233}]"
motivation: 现有LLM多智能体系统缺乏对通信拓扑的自适应优化，依赖固定结构导致效率低。
method: QueenBee Planner冻结工作池、任务适配器和评分函数，仅外LLM规划器学习生成时序通信DAG，并通过Preserve/Modify/Avoid动作从执行轨迹中提炼规则，利用多种门控防止过拟合。
result: 在CF fulltest中最佳生成图将RMSE从固定拓扑的12.53降至7.87，同时减少消息数、模型调用和token成本；Silo任务也优于冷启动和固定基线。
conclusion: 多智能体系统可以学习可重用的架构设计知识而非仅记忆答案，为高效协作提供新范式。
---

## 摘要
大型语言模型（LLM）多智能体系统越来越依赖于不仅单个智能体如何推理，而且智能体之间如何连接。本文介绍了QueenBee规划器，这是一个将智能体间通信拓扑视为可检索且自我改进的设计技能的框架。工作智能体池、任务适配器和评分函数是固定的；只有一个外部LLM规划器学习生成时序通信有向无环图（DAG），指定谁向谁发送信息、在哪一轮、谁合并消息以及谁输出最终答案。执行轨迹被提炼为基于证据的设计规则，包含三个动作：保留、修改和避免。为了防止自我演化将幸运运行或看似合理但错误的解释转化为策略，QueenBee使用了保留接受门、方差感知信用、模体级归因、迁移信任、洞察证伪和结构去重。我们在计数频率聚合和Silo-Bench式分布式协调任务上评估了该方法。在固定工作智能体的情况下，自我演化的图生成产生的通信结构优于固定拓扑和冷生成。在CF全测试设置中，最佳生成图将最强固定拓扑的RMSE从12.53降低到7.87，同时减少了消息数、模型调用和Token成本；Silo式结果显示了相对于冷生成和固定拓扑基线的相同改进方向。这些结果表明，多智能体系统可以学习可重用的架构设计知识，而不仅仅是记忆任务答案。

## Abstract
Large language model (LLM) multi-agent systems increasingly depend not only on how individual agents reason, but also on how agents are connected. This paper introduces QueenBee Planner, a framework that treats inter-agent communication topology as a retrievable and self-improving design skill. A pool of worker agents, the task adapter, and the scoring function are frozen; only an outer LLM planner learns to generate temporal communication DAGs specifying who sends information to whom, in which round, who merges messages, and who emits the final answer. Execution traces are distilled into evidence-backed design rules with three actions: \emph{Preserve}, \emph{Modify}, and \emph{Avoid}. To prevent self-evolution from turning lucky runs or plausible but false explanations into policy, QueenBee uses held-out acceptance gates, variance-aware credit, motif-level attribution, transfer trust, insight falsification, and structural deduplication. We evaluate the method on Count-Frequency aggregation and Silo-Bench-style distributed coordination tasks. With fixed workers, self-evolved graph generation produces communication structures that improve over fixed topologies and cold generation. In the CF fulltest setting, the best generated graph reduces RMSE from 12.53 for the strongest fixed topology to 7.87 while also reducing messages, model calls, and token cost; Silo-style results show the same direction of improvement over cold and fixed-topology baselines. These results suggest that multi-agent systems can learn reusable architectural design knowledge rather than merely memorizing task answers.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：现有LLM多智能体系统的性能不仅取决于单个智能体的推理能力，还取决于智能体之间的连接方式（通信拓扑）。然而，大多数系统要么使用固定拓扑，要么在特定任务上优化后即停止，缺乏跨任务可重用的通信设计知识。作者希望将通信拓扑本身作为一个可自改进的设计技能，使规划器（architect）能够随着经验增长而生成更好的通信结构，而非仅记忆任务答案。
- **整体含义**：论文从架构学习的角度重新定义多智能体系统优化——冻结工作智能体，只让外层规划器学习如何连接智能体。这使系统能够积累关于“何时保留、修改或避免”特定通信模式的设计知识，从而在不改变底层模型参数的前提下提升效率与准确性。

## 2. 论文提出的方法论

### 核心思想
- **分离规划器与工作智能体**：工作智能体池（worker pool）、任务适配器、评分函数均固定；仅一个外部LLM规划器（QueenBee Planner）学习生成时域通信有向无环图（temporal communication DAG）。
- **生成式通信DAG**：规划器输出G = (N, T, E, h, I, R)，其中N为智能体数，T为通信轮数，E为有向时域边集，h为最终答案持有者，I为接收方指令，R为合并规则。
- **设计技能库（Skill Bank）**：将执行证据提炼为三种动作：**Preserve**（保留）、**Modify**（修改）、**Avoid**（避免）。每个技能包含适用条件、设计规则、统计摘要、证据和反例。
- **自演化循环**：每个迭代中，规划器生成候选DAG，执行后收集证据，提炼技能更新，但更新需通过留出生成门（held-out gate）才能进入技能库。

### 关键技术细节
- **可行性约束**：轮数T ≤ T_max，总边数|E| ≤ M，每轮每个接收者的最大入度≤ β，确保可执行且成本可控。
- **模体级信用分配**：将每个DAG分解为8个结构模体（如通信步数、消息总数、扇入桶、是否存在审计边等），对每个模体计算带样本数惩罚的保守损失估计，用于候选排序。
- **留出生成门**：技能更新必须使规划器在留出任务和种子上的性能显著优于旧技能库（ΔJ_val ≤ -ε），避免噪声策略。
- **方差感知信用**：对每个技能计算风险损失L_risk = 均值 + κ·标准差/√n，低样本、高方差的技能难以主导。
- **洞察证伪**：LLM提出的自然语言见解先作为假设，需通过留出证据的证伪检验才能转化为技能。
- **迁移信任**：技能即使进入库中，也需根据当前任务特征槽是否有直接成功证据来决定是Preserve、Modify还是回退到冷启动。
- **结构去重**：通过时域拓扑等价哈希合并同构的DAG规范，防止技能库膨胀和信用分裂。

### 算法流程（文字说明）
1. 初始化技能库S0。
2. 对于每个演化轮次r：
   - 规划器π_Sr根据技能库生成候选DAG（通过检索技能、生成候选、可行性检查、模体+探针排序）。
   - 在训练实例和多个种子上执行DAG，收集证据行。
   - 从证据中提炼候选技能更新ΔS（包括正规则、反例规则、可执行脚手架）。
   - 应用噪声过滤和洞察证伪。
   - 在留出任务/种子上评估旧规划器π_Sr和新规划器π_S'的性能。
   - 如果留出门通过（性能显著改善），则接受更新到S_{r+1}；否则隔离ΔS。
3. 返回最终技能库S_R。

## 3. 实验设计

### 任务/数据集
- **Count-Frequency（CF）聚合**：全局数组分片给8个智能体，要求统计每个值的出现频率。答案完全可检查，RMSE、精确匹配、消息数、模型调用数、Token成本等指标。
- **Silo-Bench式分布式协调**：更复杂的任务，智能体处于信息孤岛，需协调恢复全局答案。使用6个留出案例（3个难度桶），每个案例4个种子，共24个配对实例。

### Benchmark与对比方法
- **固定拓扑**：tree、mesh-star、one-peer exponential DAG star、chain等。
- **冷生成（cold free-DAG）**：无技能库的规划器直接生成DAG。
- **自身演化**：使用不同演化轮次后的技能库生成DAG。
- **额外对比**：每案例最优固定拓扑（oracle）、不同模型组合（DeepSeek-V4-Flash、GPT-4o、Qwen3.5-Flash等）。

### 具体设置
- 智能体数：8（CF）、6（Silo，每个留出案例4种子）。
- 数组长度：1024（CF）、512（GPT-4o实验）。
- 种子集：101-104（CF）、401-404（Silo）。
- 模型：DeepSeek-V3、DeepSeek-V4-Flash、Qwen2.5-Max、Qwen3.5-Flash、GPT-4o、MiMo-V2-Flash等。

## 4. 资源与算力

论文中**未明确说明使用的GPU型号、数量或训练时长**。仅提到实验使用了多个LLM作为工作智能体和规划器（如DeepSeek、GPT-4o等），但未提供具体的硬件配置或计算预算。因此，无法在此总结算力细节。

## 5. 实验数量与充分性

### 实验组数
- CF主实验：9种模型×多个设置，包含固定拓扑、冷生成、自演化多轮对比。表1和表2展示了代表性结果。
- Silo实验：3轮演化，对比冷生成、最佳固定拓扑、每案例oracle、自演化。
- 轮次演化分析：CF和Silo各展示5轮演化轨迹（图3），并报告每轮均值、保留最优、技能库大小。
- 消融/深入分析：包含技能卡示例、模体分析、方差感知等机制的解释，但未提供系统性的消融实验（如去掉留出门、去掉模体信用等）。

### 充分性评价
- **优点**：实验覆盖了多种主流LLM（DeepSeek系列、Qwen系列、GPT-4o、MiMo），且在不同任务（CF和Silo）上验证了通用性；使用了多个种子和留出评估，控制了随机性；结果报告了RMSE和成本等多个维度。
- **不足**：
  - 未提供完整的消融实验量化各个组件（留出门、方差信用、模体归因等）的贡献。
  - Silo实验仅完成3轮演化，论文称“最终版本应添加剩余独立演化轮的均值和方差”。
  - 缺乏对更大规模智能体数（如16、32）的测试，和更长演化轮次下的收敛性分析。
  - 未与其他自动化拓扑优化方法（如GPTSwarm、AFlow、G-Designer）直接比较。
- **总体公平性**：对比方法包括常见固定拓扑和冷生成基线，但缺少同类拓扑学习系统的对比。实验结果倾向于自演化，但提升幅度在不同模型上有所波动，论文也承认冷生成不稳定。

## 6. 论文的主要结论与发现

- 在CF全测试设置下，自演化生成的最佳图（混合reduce-audit结构）将RMSE从最强固定拓扑的12.53降至7.87（降幅37.2%），同时消息数减少74.2%，模型调用减少54.5%，Token成本减少59.6%。
- 在Silo任务上，3轮演化后EM从冷生成的0.125提升至0.539，超越最佳固定拓扑（0.333）和每案例oracle固定拓扑（0.417）。
- 演化过程显示规划器能积累可迁移的结构偏好（如分阶段聚合、低扇入、审计边），且改善并非来自工作智能体的个体能力提升，而是来自通信结构设计知识的积累。
- 技能库可以包含正规则（保留）和负规则（避免），并且可以通过留出门避免噪声干扰。

## 7. 优点

### 方法层面
- **新颖的问题框架**：将通信拓扑重定义为可自改进的设计技能，冻结工作层，使架构学习成为独立问题。
- **完善的自演化纪律**：留出门、方差感知信用、模体级归因、洞察证伪、结构去重等机制有效防止过拟合和噪声积累，提升了可信任度。
- **生成式DAG表示**：时域有向无环图支持灵活的通信模式，且可行性约束确保实际可执行。
- **技能的可解释性**：设计规则以自然语言和结构化证据存储，便于人类理解迁移。

### 实验层面
- **多模型验证**：覆盖DeepSeek、Qwen、GPT-4o、MiMo等主流LLM，增强通用性。
- **双任务验证**：内部有效性（CF精确可检查）和外部有效性（Silo更复杂）结合。
- **成本考量**：不仅报告准确率，还报告消息数、模型调用、Token成本，体现token效率。

## 8. 不足与局限

### 实验覆盖不足
- **缺乏与同类方法的对比**：未与GPTSwarm、AFlow、G-Designer等直接优化拓扑的系统比较。
- **消融实验不完整**：没有单独评估留出门、方差信用、模体归因等组件的重要性。
- **规模有限**：仅使用8个工作智能体，未扩展到更大智能体群（如16、32）。
- **演化轮次少**：Silo仅3轮，CF最多5轮，长程演化的稳定性未知。

### 偏差风险
- **任务选择偏差**：CF和Silo均为分布式聚合类任务，对通信拓扑敏感；但其他类型任务（如开放域对话、代码生成）的泛化性未验证。
- **LLM作为规划器的内在偏差**：规划器本身也是LLM，其生成可能受自身倾向影响（如偏好某种拓扑命名），论文未消除此风险。

### 应用限制
- **技能库维护开销**：随着技能数量增长，检索和匹配可能成为瓶颈；论文仅提到去重机制，未讨论扩展性。
- **依赖外部评分函数**：需要精确的L(ŷ, y)和成本函数，对于开放式任务难以定义。
- **冷启动问题**：初期无技能时，冷生成性能较差（CF 16.63 RMSE vs 固定12.53），实际部署可能需要预设若干基础技能。

（完）
